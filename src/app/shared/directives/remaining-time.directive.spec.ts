import { RemainingTimeDirective } from './remaining-time.directive';

describe('RemainingTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new RemainingTimeDirective();
    expect(directive).toBeTruthy();
  });
});
